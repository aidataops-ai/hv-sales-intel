.superpowers/
.env
.env.local
node_modules/
__pycache__/
*.pyc
.vercel

# macOS metadata files
.DS_Store

# Source ICP PDFs — internal reference, not for the demo repo
ICP Documents/

# Leadset working files (build_leadset / run_enrich_push generate these per run)
leadset-*.json

# Raw Clay webhook callbacks captured while mapping their field names.
# Contains prospect PII — never commit.
clay-webhook-captures.jsonl
